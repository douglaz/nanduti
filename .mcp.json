{
  "mcpServers": {
    "nanduti": {
      "command": "cargo",
      "args": ["run", "--features", "mcp", "--", "mcp-server"],
      "env": {
        "RUST_LOG": "info",
        "API_HOST": "localhost",
        "API_PORT": "3517"
      }
    }
  }
}